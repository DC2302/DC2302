{
  "name": "phone-answering-service",
  "version": "0.1.0",
  "description": "Multi-tenant AI phone answering service. Each client company gets its own folder of training materials that the agent uses to answer callers' questions.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "sync:gdrive": "node scripts/sync-gdrive.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "twilio": "^5.3.0"
  }
}
